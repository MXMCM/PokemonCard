extends layout

block content
  include mixins/Card
    div.container
      div.add-button-row
        a.button.save(href="/add") Add Card
    div.container
      each PokemonCard in PokemonTable
        +Card(PokemonCard)